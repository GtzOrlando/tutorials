<?xml version="1.0"?>
<odoo>
    <!-- Vista de lista -->
    <record id="view_estate_property_list" model="ir.ui.view">
        <field name="name">estate.property.list</field>
        <field name="model">estate.property</field>
        <field name="arch" type="xml">
            <list string="Properties" create="true">
                <field name="name" string="Title" width="200px"/>
                <field name="postcode" width="100px"/>
                <field name="bedrooms" width="100px"/>
                <field name="living_area" string="Living Area (sqm)" width="150px"/>
                <field name="expected_price" widget="monetary" width="150px"/>
                <field name="selling_price" widget="monetary" width="150px"/>
                <field name="date_availability" string="Available From" width="150px"/>
            </list>
        </field>
    </record>

    <!-- Vista de formulario -->
    <record id="view_estate_property_form" model="ir.ui.view">
        <field name="name">estate.property.form</field>
        <field name="model">estate.property</field>
        <field name="arch" type="xml">
            <form string="Estate Property">
                <sheet>
                    <h1>
                        <field name="name" string="Title"/>
                    </h1>
                    <group>
                        <group>
                            <field name="postcode"/>
                            <field name="date_availability"/>
                        </group>
                        <group>
                            <field name="expected_price" widget="monetary"/>
                            <field name="selling_price" widget="monetary"/>
                        </group>
                    </group>
                    <notebook>
                        <page string="Description">
                            <group>
                                <field name="description"/>
                                <field name="bedrooms"/>
                                <field name="living_area" string="Living Area (sqm)"/>
                                <field name="facades"/>
                                <field name="garage"/>
                                <field name="garden"/>
                                <field name="garden_area" string="Garden Area (sqm)"/>
                                <field name="garden_orientation"/>
                                <field name="active"/>
                                <field name="state"/>
                            </group>
                        </page>
                    </notebook>
                </sheet>
            </form>
        </field>
    </record>

    <!-- Vista de búsqueda -->
    <record id="view_estate_property_search" model="ir.ui.view">
        <field name="name">estate.property.search</field>
        <field name="model">estate.property</field>
        <field name="arch" type="xml">
            <search>
                <!-- Campos de búsqueda -->
                <field name="name"/>
                <field name="postcode"/>
                <field name="expected_price"/>
                <field name="bedrooms"/>
                <field name="living_area"/>
                <field name="facades"/>

                <separator/>
                <!-- Filtros rápidos -->
                <filter name="filter_available" string="Available" domain="['|',('state','=','new'),('state','=','offer_received')]"/>

                <!-- Group By -->
                <filter name="group_by_postcode" string="Postcode" context="{'group_by': 'postcode'}"/>

            </search>
        </field>
    </record>

    <!-- Acción -->
    <record id="action_estate_property" model="ir.actions.act_window">
        <field name="name">Properties</field>
        <field name="res_model">estate.property</field>
        <field name="view_mode">list,form</field>
        <field name="view_id" ref="view_estate_property_list"/>
        <field name="context">{'active_test': False}</field>
    </record>

</odoo>